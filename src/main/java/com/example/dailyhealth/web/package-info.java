package com.example.dailyhealth.web;

/**
 * Here we have our controllers.
 * Things ToDo:
 * 1.Optimize controller.
 * 2.Change variable names to more suitable.
 * 3.Add error handling on controller level.
 * 4.Add validation on controller level. I will add validation annotations on DTOs, and validate incoming data before
 * sending it to service layer.
 */